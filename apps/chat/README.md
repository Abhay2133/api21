# @api21/chat

Chat application skeleton for the `api21` monorepo.

## Overview
This workspace is reserved for the upcoming chat service / client application. It connects to the core `@api21/api` backend and shares types directly via `@api21/types`.

## Scripts
- `pnpm dev`: Run in watch mode.
- `pnpm build`: Build the TypeScript project.
