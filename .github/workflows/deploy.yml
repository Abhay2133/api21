name: Deploy

on:
  push:
    branch:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install Dependencies
        uses: bun install

      - name: Test Cases
        uses: bun Test

      - name: Deploy to Render
        uses: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}