# API21 Environment Configuration
# Copy this file to .env and modify the values as needed

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# Database URL for production PostgreSQL
# If DATABASE_URL is set, PostgreSQL will be used (production mode)
# If DATABASE_URL is not set, SQLite will be used (development mode)
# Example PostgreSQL URL format:
# DATABASE_URL=postgres://username:password@localhost:5432/database_name

# Development mode (SQLite) - No configuration needed
# SQLite database file will be created as api21.db

# ===========================================
# CRON JOB CONFIGURATION
# ===========================================

# URL Ping Job Configuration
# Set PING_URL to enable the URL health check ping job
# The job will send GET requests to this URL at the specified interval
PING_URL=https://httpbin.org/get

# Ping interval in minutes (must be a positive integer)
# This determines how often the ping job runs
PING_INTERVAL=5

# ===========================================
# SERVER CONFIGURATION (Future Use)
# ===========================================

# Server port (default: 3000)
# PORT=3000

# Environment mode
# ENV=development

# ===========================================
# DATABASE CONFIGURATION (Future Use)
# ===========================================

# Database connection string
# DATABASE_URL=postgresql://user:password@localhost:5432/api21

# ===========================================
# LOGGING CONFIGURATION (Future Use)
# ===========================================

# Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Log format (json, text)
# LOG_FORMAT=text